_target_: src.data.ml_datamodule.MLDataModule
data_dir: ${paths.data_dir}
text_pretrained: bert-base-uncased
train_val_ratio: [0.85, 0.15]
batch_size: 32
num_workers: 0
pin_memory: false
transforms: null